[package]
name = "cachewipe"
version = "0.1.0"
edition = "2021"
description = "Fast, safe reclaimer of regenerable cache and build files. Reports by default, deletes only on --apply."
license = "MIT"
repository = "https://github.com/longwind48/cachewipe"
readme = "README.md"
keywords = ["cache", "disk", "cleanup", "storage", "prune"]
categories = ["command-line-utilities", "filesystem"]

[[bin]]
name = "cachewipe"
path = "src/main.rs"

[dependencies]
# Kept deliberately small so the whole tree stays auditable.
serde = { version = "1", features = ["derive"] }
serde_json = "1"
# Parallel directory walk. A single cache dir is often the bulk of a scan, and a
# single-threaded walk over it measured slower than `du`; see src/scan.rs.
jwalk = "0.8"

[profile.release]
opt-level = 3
lto = true
strip = true
panic = "abort"
